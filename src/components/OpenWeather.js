import {Row} from 'reactstrap';

const OpenWeather = () => {
    return(
        <footer>
            <Row>
                <p>Open Weather</p>
            </Row>
        </footer>
    );
};

export default OpenWeather;